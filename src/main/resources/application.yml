spring:
  application:
    name: aux-area
  config:
    import: optional:file:.env-local[.properties]
#  datasource:
#    url: jdbc:h2:file:./data/h2-data;DB_CLOSE_ON_EXIT=FALSE;IFEXISTS=TRUE;DB_CLOSE_DELAY=-1
#    username: sa
#    password:
server:
  port: 8080

# this is hardcoded because it is only applicable in local environment (will be environment variable in heroku deployment)
jwt:
  # change this to environment variable
  secret:
  expiration: 3600000

#
